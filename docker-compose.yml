services:
  tracker-worker:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - '9091:9091'
    volumes:
      - type: bind
        source: /mnt/wd/feditracker-storage
        target: /var/www/html/wd
    env_file:
      - .env
